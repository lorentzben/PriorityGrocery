/*package com.example.android.prioritygroceryempty;


import android.os.Bundle;
import android.app.Activity;
import android.view.Menu;
import android.widget.ArrayAdapter;
import android.widget.ListView;


import java.util.AbstractMap;
import java.util.List;
import java.util.Map;
import java.util.PriorityQueue;



/*
public class ListDisplay extends Activity {
/*
    PriorityQueue<Map.Entry<String,Integer>> items = new PriorityQueue<>(1, new DescendingByPriority());

    items.offer(new AbstractMap.SimpleEntry<String, Integer>("butter",Integer.valueOf(3)));
    items.offer(new AbstractMap.SimpleEntry<String, Integer>("beer",Integer.valueOf(5)));
    items.offer(new AbstractMap.SimpleEntry<String, Integer>("cheese",Integer.valueOf(1)));

    @Override
    protected void onCreate(Bundle savedInstanceState){
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        GroceryAdapter adapter = new GroceryAdapter(this, items);
        ListView listView = (ListView) findViewById(R.id.grocery_list_view);
        listView.setAdapter(adapter);
    }
}
*/